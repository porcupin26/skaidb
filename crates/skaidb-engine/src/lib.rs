//! skaidb query execution engine: binds planned SQL to the storage engine.
//!
//! Implementation lands in a later phase; this crate currently reserves the
//! module boundary between the SQL frontend and the storage layer.
