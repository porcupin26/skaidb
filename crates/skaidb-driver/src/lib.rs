//! skaidb client driver: topology-aware, two-level routing (token map then
//! least-loaded member), binary + REST endpoints (SPEC §7).
//!
//! Implementation lands in a later phase; this crate currently reserves the
//! module boundary.
